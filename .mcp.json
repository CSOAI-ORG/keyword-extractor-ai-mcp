{
  "name": "keyword-extractor-ai-mcp",
  "description": "Keyword Extractor Ai tools for AI agents. Capabilities: extract keywords, extract phrases, topic classification. Built by MEOK AI Labs.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "extract_keywords",
      "description": "Extract top keywords using TF-IDF scoring.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "max_keywords": {
            "type": "integer"
          }
        },
        "required": [
          "text"
        ]
      }
    },
    {
      "name": "extract_phrases",
      "description": "Extract key phrases (2-3 word combinations).",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "max_phrases": {
            "type": "integer"
          }
        },
        "required": [
          "text"
        ]
      }
    },
    {
      "name": "topic_classification",
      "description": "Classify text into topic categories.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ]
      }
    },
    {
      "name": "keyword_density",
      "description": "Calculate keyword density for SEO analysis.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ]
      }
    }
  ]
}